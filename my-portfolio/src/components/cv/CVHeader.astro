---
import cvHeader from "../../data/cv.json";
---

<div
  class="cv-header-container md:max-w-[100%] md:flex md:flex-row md:flex-between border-b-1 border-black-100 bg-[#403d39] text-white print:bg-white print:text-black"
>
  <div class="cv-header-profile md:max-w-[100%] p-5">
    <h1 class="text-xl sm:text-2xl md:text-3xl font-bold text-center mb-4">
      {cvHeader.cvHeader.fullNameAllCaps}
    </h1>
    <p class="md:text-xl sm:text-lg font-semibold">{cvHeader.cvHeader.title}</p>
    <p class="text-sm">
      {cvHeader.cvHeader.shortDescription}
    </p>
  </div>
</div>

<style>
  @media print {
    .border-r-1,
    .border-b-1 {
      border: 0px;
    }
    .cv-header-profile {
      margin: 0;
    }

    .cv-header-container {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      max-width: 100%;
    }
  }
</style>
