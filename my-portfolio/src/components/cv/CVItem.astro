---
import IconTextAlign from "./IconTextAlign.astro";
interface Props {
  name: string;
  date: string;
  technologies?: string;
  jobTitle?: string;
  universityName?: string;
  description: string | string[];
}

const {
  name,
  date,
  technologies,
  jobTitle,
  description,
  universityName,
}: Props = Astro.props;
---

<div>
  <div class="flex flex-col md:flex-row md:justify-between md:items-center">
    <div>
      <p
        class="title font-semibold md:text-lg sm:text-base text-gray-900 dark:text-white print:text-gray-900"
      >
        {name}
      </p>
      {
        technologies && (
          <div class="text-xs text-blue-700 dark:text-blue-400 print:text-blue-700">
            {technologies}
          </div>
        )
      }
      {
        jobTitle && (
          <p class="text-sm text-gray-700 dark:text-gray-300 print:text-gray-700">
            <i>{jobTitle}</i>
          </p>
        )
      }
      {
        universityName && (
          <p class="text-xs text-gray-700">
            <IconTextAlign iconName="location" text={universityName} />
          </p>
        )
      }
    </div>
    <span class="text-xs text-gray-500 dark:text-gray-400 print:text-gray-500">
      <IconTextAlign iconName="calendar" text={date} /></span
    >
  </div>
  {
    Array.isArray(description) ? (
      <ul class="list-disc ml-5 text-sm text-gray-900 dark:text-white print:text-gray-900">
        {description.map((item) => (
          <li>{item}</li>
        ))}
      </ul>
    ) : (
      <p class="text-sm text-gray-900 dark:text-white print:text-gray-900">
        {description}
      </p>
    )
  }
</div>
